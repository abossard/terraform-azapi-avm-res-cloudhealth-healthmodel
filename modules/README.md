# Child resource submodules

- `authentication-setting` manages one managed-identity authentication setting.
- `entity` manages one health model entity.
- `signal-definition` manages one reusable signal definition.
- `relationship` manages one entity relationship.
- `discovery-rule` manages one discovery rule.

Each submodule can be consumed independently with an existing CloudHealth health model resource ID.
